import java.util.Collections;
import java.util.List;

public class SearchService {
    public List<String> search(String query) {
        // Instead of real search logic, force return an empty list
        return Collections.emptyList();
    }
}
